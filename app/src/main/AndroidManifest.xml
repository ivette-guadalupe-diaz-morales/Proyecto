<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="igdm.itics.tesoem.edu.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".valsActivity"></activity>
        <activity android:name=".sonesActivity" />
        <activity android:name=".chotisActivity" />
        <activity android:name=".redobasActivity" />
        <activity android:name=".polkasActivity" />
        <activity android:name=".jaranasActivity" />
        <activity android:name=".jarabesActivity" />
        <activity android:name=".chilenasActivity" />
        <activity android:name=".danzaActivity" />
        <activity android:name=".queActivity" />
        <activity android:name=".menuActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>